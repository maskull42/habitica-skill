# Dependency for the OPTIONAL MCP server (mcp/habitica_mcp.py) only.
# The Claude Code skill in skills/habitica needs nothing installed.
#
# Official Model Context Protocol Python SDK — provides mcp.server.fastmcp.FastMCP.
# Latest version: https://pypi.org/project/mcp/
mcp>=1.2.0
